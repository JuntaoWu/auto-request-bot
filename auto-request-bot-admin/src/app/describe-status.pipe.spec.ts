import { DescribeStatusPipe } from './describe-status.pipe';

describe('DescribeStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new DescribeStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
